destination: state
schema:
  title: MockRequest
  type: object
  description: Request for testing business logic
  properties:
    number:
      description: Number of Request
      type: string
    overrides:
      $ref: "#/$defs/overrides"
  required:
    - number
  additionalProperties: false
